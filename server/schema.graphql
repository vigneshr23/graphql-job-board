type Query {
  jobs: [Job]
  company: Company
}

type Job {
  id: ID!
  title: String
  description: String
  company: Company
}

type Company {
  id: ID!
  name: String
  description: String
  type: String
}
